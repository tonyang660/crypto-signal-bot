name: Trading Signal Bot

on:
  schedule:
    # Run every 5 minutes - Active and working
    - cron: '*/5 * * * *'
  
  workflow_dispatch:  # Allow manual trigger from Actions tab
    inputs:
      force_scan:
        description: 'Force a market scan'
        required: false
        default: 'true'
  
  push:
    branches: [master]
    paths:
      - 'src/**'
      - 'requirements.txt'
      - '.github/workflows/**'

jobs:
  run-signal-bot:
    runs-on: cirun-aws--${{ github.run_id }}
    timeout-minutes: 10
    environment: signal-bot  # Use the environment you created
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 🔧 Install AWS CLI
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y unzip
          curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
          unzip -q awscliv2.zip
          sudo ./aws/install
          aws --version
      
      - name: ☁️ Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}
        
      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: 📁 Create directories
        run: |
          mkdir -p data logs
      
      - name: ☁️ Download state from S3
        run: |
          echo "📥 Downloading state files from S3..."
          aws s3 sync s3://${{ secrets.AWS_S3_BUCKET }}/state/ data/ \
            --exclude "*" \
            --include "*.json" \
            --no-progress || echo "⚠️ No existing state found (first run?)"
          
          # List downloaded files
          ls -lah data/ || true
      
      - name: 🤖 Run signal bot
        env:
          BITGET_API_KEY: ${{ secrets.BITGET_API_KEY }}
          BITGET_SECRET_KEY: ${{ secrets.BITGET_SECRET_KEY }}
          BITGET_PASSPHRASE: ${{ secrets.BITGET_PASSPHRASE }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          INITIAL_CAPITAL: ${{ vars.INITIAL_CAPITAL }}
          RISK_PER_TRADE: ${{ vars.RISK_PER_TRADE }}
          MAX_DAILY_LOSS: ${{ vars.MAX_DAILY_LOSS }}
          MAX_WEEKLY_LOSS: ${{ vars.MAX_WEEKLY_LOSS }}
        run: |
          echo "🚀 Starting signal bot scan..."
          python -m src.main --single-run
      
      - name: ☁️ Upload state to S3
        if: always()  # Always upload, even if bot fails
        run: |
          echo "📤 Uploading state files to S3..."
          aws s3 sync data/ s3://${{ secrets.AWS_S3_BUCKET }}/state/ \
            --exclude "*" \
            --include "*.json" \
            --no-progress
          
          # Also backup to timestamped folder (weekly snapshots)
          if [ "$(date +%u)" -eq 1 ]; then
            echo "📸 Creating weekly backup..."
            aws s3 sync data/ s3://${{ secrets.AWS_S3_BUCKET }}/backups/$(date +%Y-%m-%d)/ \
              --exclude "*" \
              --include "*.json" \
              --no-progress
          fi
      
      - name: 📤 Upload state as artifacts (backup)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bot-state-backup
          path: |
            data/signals_active.json
            data/signals_history.json
            data/performance.json
          retention-days: 7
      
      - name: 📊 Upload logs as artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bot-logs-${{ github.run_number }}
          path: logs/
          retention-days: 7
      
      - name: ⚠️ Notify on failure
        if: failure() && env.DISCORD_WEBHOOK_URL != ''
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          curl -H "Content-Type: application/json" \
               -d "{\"content\":\"⚠️ **Trading Bot Workflow Failed**\n\nRun: ${{ github.run_number }}\nTime: $(date -u)\n[View Logs](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})\"}" \
               ${{ secrets.DISCORD_WEBHOOK_URL }}